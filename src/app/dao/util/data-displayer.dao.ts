export enum DataType {
    TEXT,
    DATE,
    TIME,
    IMAGE,
    TEMPERATURE,
    PRECENTAGE,
    PREASSURE,
    HEIGHT,
    SPEED
}

export enum DataMeasurementsDegree {
    TEMPERATURE = "Celcius",
    PERCENTAGE = "%",
    PREASSURE = "hPa",
    HEIGHT = "mm",
    SPEED = "m/s"
}