import { OpenWeatherImagePipe } from './open-weather-image.pipe';

describe('OpenWeatherImagePipe', () => {
  it('create an instance', () => {
    // const pipe = new OpenWeatherImagePipe();
    // expect(pipe).toBeTruthy();
  });
});
